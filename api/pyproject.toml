[project]
name = "api"
version = "0.1.0"
description = "API for the PetCare app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.135.1",
    "firebase-admin>=7.3.0",
    "pymongo>=4.16.0",
    "pytest>=9.0.2",
    "uvicorn>=0.42.0",
]
