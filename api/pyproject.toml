[project]
name = "api"
version = "0.1.0"
description = "API for the PetCare app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.135.1",
    "pymongo>=4.16.0",
    "uvicorn>=0.42.0",
]
