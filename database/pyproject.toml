[project]
name = "database"
version = "0.1.0"
description = "Seed script for the database"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "pymongo>=4.16.0",
]
